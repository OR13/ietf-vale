# Protocol names

The Http client uses Tls over Ipv6. The Dns resolver sends a Udp query, and a
Quic endpoint can use Websocket framing.

These names must stay clean: HTTP, TLS, IPv6, DNS, UDP, QUIC, and WebSocket.

`Http` and `Tls` in code examples must stay clean too.
