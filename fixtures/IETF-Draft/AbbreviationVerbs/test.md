# AbbreviationVerbs

Punctuated suffixes are wrong: the value is XOR'ed, the address is NAT-ed, and
the packet is being NAT-ing.

These must stay clean: XORed, NATed, MACs, the possessive form in the RPC's
list, and the plain abbreviations XOR and NAT on their own.
