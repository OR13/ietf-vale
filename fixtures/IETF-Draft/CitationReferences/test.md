# CitationReferences

The matching citation [RFC8174] is defined below.
The citation [RFC2119] needs a reference definition.
An inline link [RFC Editor](https://www.rfc-editor.org/) is already complete.
An inline code citation `[RFC9998]` is not prose.

```text
[RFC9997] is an example inside a code block.
```

[RFC8174]  BCP 14 and the use of requirement terms.
