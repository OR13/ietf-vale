# ExampleASN

For example, use AS64512 for the network.
For documentation, use AS64500 instead.

These must stay clean: the deployed network uses AS64512, and section 64512
is not an example AS number.
