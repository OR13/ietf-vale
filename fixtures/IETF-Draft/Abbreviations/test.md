# Abbreviations

The Transmission Control Protocol (TCP) is defined here, and TCP is well known
after its expansion in this sentence.

The API sends a request and should be expanded in this sentence.

These must stay clean: HTTP, IETF, RFC, TCP, and XML are well known.
